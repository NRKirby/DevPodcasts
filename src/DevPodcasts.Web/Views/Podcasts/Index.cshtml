@model DevPodcasts.ViewModels.Podcast.PodcastIndexViewModel
@{
    ViewBag.Title = "Podcasts";
    var episodeCount = Model.EpisodeCount;
}

<div class="podcasts-container">
    
    <h2>Podcasts</h2>

    <div style="margin: 25px 0;">
        @if (Model == null)
        {
            <h4>0 podcasts</h4>
        }
        else if (Model.Items.Count() == 1)
        {
            <h4>1 podcast</h4>
        }
        else
        {
            <h4>@Model.Items.Count() podcasts</h4>
        }
        <h4>@episodeCount episodes</h4>
    </div>

    <div id="results-container">
        @if (Model != null)
        {
            <hr />
            foreach (var result in Model.Items)
            {
                <div class="row result-row" id="@result.Id">
                    <div class="col-sm-2 hidden-xs">
                        <img class="result-img" src="@result.ImageUrl" />
                    </div>
                    <div class="col-sm-10">
                        <h3>@result.Title</h3>
                        <p class="">@result.Description</p>
                        <div class="row">
                            <div class="podcast-tag-list col-sm-10">
                                @if (result.Tags != null)
                                {
                                    foreach (var tag in result.Tags)
                                    {
                                        <a href="~/tags/@tag.Slug" class="podcast-tag" title="Podcasts tagged @tag.Name">@tag.Name</a>
                                    }
                                }
                            </div>
                            <div class="col-sm-2"><p class="pull-right"><span class="result-episode-count">@result.NumberOfEpisodes episodes</span></p></div>
                        </div>
                    </div>
                </div>
                <hr />
            }
        }
    </div>

</div>



<script>
    $(document).ready(function () {
        $(".result-row").click(function () {
            var id = this.id;
            document.location.href = getURL() + "podcasts/detail/" + id;
        })
    });

    function getURL() {
        var arr = window.location.href.split("/");
        delete arr[arr.length - 1];
        return arr.join("/");
    }
</script>